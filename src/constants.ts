const BASE= 'composable-grid';

export const STYLES = {
    HEIGHT_ZERO: {height: '0px'}
};

export const CLASSES = {
    GRID: BASE,
    ROW_GROUP: `${BASE}-rowgroup`,
    ROW: `${BASE}-row`,
    COLUMN: `${BASE}-column`
};

export const DEFAULT_ROW_HEIGHT = 30;
export const DEFAULT_COLUMN_WIDTH = 200;
